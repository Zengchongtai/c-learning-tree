/*



WPL最小的二叉树叫做哈夫曼树

如何构造哈夫曼树？

// 排序时小的放左边,大的放右边,相等无所谓
1.先把有权值的叶子结点按照从小到大的顺序排列成一个有序序列
2.取两个最小权值的结点作为一个新结点N1的子结点(N1的权值为两个最小权值相加)
3.将结点N1替换刚刚取出的两个结点,并加入到有序序列中(记得重新排一次大小)
4.重复步骤2,3
5.最后均展开


哈夫曼树构造示例：
假设有以下权值的叶子节点：{5, 5, 10, 15, 20}

步骤1：将所有节点按权值从小到大排序
初始序列：5, 5, 10, 15, 20

步骤2：取出两个最小权值节点(5, 5)构造新节点N1
- 取出两个权值为5的节点
- 创建新节点N1,权值 = 5 + 5 = 10
- 将这两个5节点作为N1的左右子树
- 将N1(权值10)加入序列,重新排序
- 序列变为：10, 10(N1), 15, 20

步骤3：取出两个最小权值节点(10, 10(N1))构造新节点N2
- 取出权值为10的原始节点和N1节点
- 创建新节点N2,权值 = 10 + 10 = 20
- 将这两个节点作为N2的左右子树
- 将N2(权值20)加入序列,重新排序
- 序列变为：15, 20, 20(N2)

步骤4：取出两个最小权值节点(15, 20)构造新节点N3
- 取出权值为15的节点和权值为20的原始节点
- 创建新节点N3,权值 = 15 + 20 = 35
- 将这两个节点作为N3的左右子树
- 将N3(权值35)加入序列,重新排序
- 序列变为：20(N2), 35(N3)

步骤5：取出最后两个节点(20(N2), 35(N3))构造根节点ROOT
- 取出权值为20的N2节点和权值为35的N3节点
- 创建根节点ROOT,权值 = 20 + 35 = 55
- 将这两个节点作为ROOT的左右子树
- 序列为空,构造完成

最终哈夫曼树结构：
        ROOT(55)
       /        \
    N2(20)     N3(35)
   /  \        /    \
  10   N1(10) 15    20
      /  \
     5    5

计算WPL(带权路径长度)：
WPL = 10×2 + 5×3 + 5×3 + 15×2 + 20×2 = 20 + 15 + 15 + 30 + 40 = 120

通过让权值大的节点尽可能靠近根节点(深度小),权值小的节点尽可能远离根节点(深度大),从而最小化WPL


哈夫曼编码: 哈夫曼树,左分支小,右分枝大



// 树与二叉树的转换
加线：在所有兄弟节点之间加一条连线
抹线：对树中每个节点,只保留它与第一个孩子节点的连线,删除它与其他孩子节点之间的连线
调整：以根节点为轴心,将整棵树顺时针旋转45度

// 森林转换为二叉树
将森林中的每棵树都转换为二叉树
将第一棵二叉树保持不动
从第二棵二叉树开始,依次将后一棵二叉树的根作为前一棵二叉树根的右孩子


*/